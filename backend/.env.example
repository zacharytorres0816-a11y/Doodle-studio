PORT=8787
DATABASE_URL=postgresql://postgres:password@127.0.0.1:5432/photo_printing_app
FRONTEND_ORIGIN=http://localhost:8080,https://dignia.org,https://www.dignia.org
# Optional: set this to your public API origin (ngrok/custom domain), never localhost.
# If omitted, backend auto-detects host from forwarded headers.
PUBLIC_BASE_URL=
